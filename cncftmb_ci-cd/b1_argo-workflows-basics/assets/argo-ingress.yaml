apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: argo
  namespace: argo
spec:
  rules:
  - http:
      paths:
        - path: "/"
          pathType: Prefix
          backend:
            service:
              name: argo-server
              port:
                number: 2746